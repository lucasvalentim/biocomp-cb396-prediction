# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Dados e artefatos (baixados/derivados pelo pipeline) — não versionar.
# Tudo é regenerável via `uv run cb396 all`.
data/

# Documentação e artigo — mantidos apenas localmente/internamente.
docs/
artigo/
instructions.md
